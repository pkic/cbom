{
  "$comment": "Fixture. Deliberately fails C6: rule I1 requires 'pqcPosture', a derived judgement whose criteria sit outside the document and change with guidance. This reproduces the attribute removed by decision 0002. The facts a posture is derived from belong in the CBOM; the posture itself belongs to external versioned policy.",
  "profileId": "pkic.test.c6",
  "profileTag": "c6",
  "title": "Fixture - C6 requires a derived judgement",
  "version": "0.1",
  "objective": {
    "consumer": "A fixture, standing in for a consumer.",
    "decision": "Whether the checker rejects an attribute that asserts a conclusion.",
    "decisionOptions": [
      "accept the fixture",
      "reject the fixture"
    ]
  },
  "appliesTo": {
    "cyclonedx": {
      "min": "1.6",
      "tested": "1.7"
    }
  },
  "interfaceTypeVocabulary": [
    "management",
    "service"
  ],
  "productRules": [
    {
      "id": "P1",
      "description": "Product declares at least one cryptographic interface",
      "level": "MUST",
      "constraint": {
        "minInterfaces": 1
      }
    }
  ],
  "interfaceRules": [
    {
      "id": "I1",
      "attribute": "pqcPosture",
      "level": "MUST",
      "constraint": {
        "enum": [
          "classical",
          "hybrid",
          "pqc"
        ]
      },
      "withholdable": false
    }
  ],
  "scope": {
    "subjectType": "product",
    "orientation": "inventory",
    "relationshipTypes": [
      "interface"
    ],
    "lifecycleStages": [
      "intended",
      "implemented",
      "configured",
      "observed"
    ]
  }
}
